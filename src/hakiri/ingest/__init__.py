"""rpc ingestion. mempool, builder bids, block traces.

most production work for these lives in `ingest-rs/` (rust). the python
modules here are reference implementations and the path used in demo mode.
"""
