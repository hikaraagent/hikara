[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[project]
name = "hakiri"
version = "0.0.1"
description = "ethereum mev forensics agent"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors  # py 3.9 floor confirmed
authors = [{ name = "hakiri" }]

[tool.hatch.build.targets.wheel]
packages = ["src/hakiri"]
